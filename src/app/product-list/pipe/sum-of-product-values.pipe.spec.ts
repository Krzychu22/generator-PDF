import { SumOfProductValuesPipe } from './sum-of-product-values.pipe';

describe('SumOfProductValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new SumOfProductValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
