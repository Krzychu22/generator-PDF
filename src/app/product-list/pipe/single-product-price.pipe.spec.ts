import { SingleProductPricePipe } from './single-product-price.pipe';

describe('SingleProductPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SingleProductPricePipe();
    expect(pipe).toBeTruthy();
  });
});
